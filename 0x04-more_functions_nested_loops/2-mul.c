#include "main.h"
#include <stdio.h>

/**
 * int mul - multiply 2 integer
 * @c: mul to multiply 
 * @a: first integar
 * @b: second integar
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a*b);
}
